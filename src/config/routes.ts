const index = '/';

const login = `${index}login`;
const create = `${index}create`;
const update = `${index}update`;
const search = `${index}search`;

export const ROUTES = {
  login,
  index,
  create,
  update,
  search
};
